<!-- Don't need this because we're just creating a data entry form:

    ATSYMBOLHEREmodel WebApplication1.Models.Joke
    -->

<h4>Search for a joke</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <!-- Change the action to the new method we will create -->
        <form asp-action="ShowSearchResults">
            <!--Don't need this, it's for validatoin:
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            -->
            <div class="form-group">
                <!-- Changed the asp-for to name, to stop viewing the model of 'Jokes'-->
                <label for="SearchPhrase" class="control-label"></label>
                <input name="SearchPhrase" class="form-control" />
                <!-- Needed for validation, we don't need this:
                    <span asp-validation-for="JokeAnswer" class="text-danger"></span>
                -->
            </div>
            <div class="form-group">
                <input type="submit" value="Search" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

<!-- Needed to validate data in correct format...not needed:

    ATSYMBOLHEREsection Scripts {
    ATSYMBOLHERE{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

    -->
